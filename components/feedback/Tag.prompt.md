Tappable pill chip (`configurator-option-chip` in the source) — used for pickable options: page ranges, chunking modes, table formats.

```jsx
<Tag selected={mode==="fast"} onClick={() => setMode("fast")}>Fast (pdfplumber)</Tag>
```
