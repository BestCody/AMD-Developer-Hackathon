Plain rectangular dropdown — an Aperture addition; the source has no select component.

```jsx
<Select options={[{value:"en",label:"English"},{value:"es",label:"Spanish"}]} value={lang} onChange={e => setLang(e.target.value)} />
```
