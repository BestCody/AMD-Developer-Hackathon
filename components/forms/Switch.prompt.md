Toggle switch — Aperture addition, for binary pipeline settings (e.g. "Skip Weaviate upsert").

```jsx
<Switch checked={withEmbeddings} onChange={setWithEmbeddings} label="Generate embeddings" />
```
