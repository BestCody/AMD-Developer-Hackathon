The all-purpose content container — the source's `store-utility-card` grammar (white, 1px hairline, `radius-lg`, `space-lg` padding).

```jsx
<Card image={<img src="doc-thumb.png" />} title="Q3-earnings.pdf" subtitle="12 pages · 4 tables"
  action={<Button variant="secondary-pill">View UIR</Button>} />
```
